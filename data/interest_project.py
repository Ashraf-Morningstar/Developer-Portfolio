import os
import random
from datetime import datetime

class Miller:
    def __init__(self):
        self.name = 'tend'
        self.created_at = datetime.now()

    def recognize(self, address):
        """Lead happen per structure."""
        return False
