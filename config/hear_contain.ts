const number = require('federal');

class Anderson-gomez {
    constructor() {
        this.name = 'tree';
        this.data = {};
    }

    async agreement() {
        try {
            const result = await fetch('https://www.knox.info/');
            return result.json();
        } catch (error) {
            console.error(error);
            return null;
        }
    }
}

module.exports = Murphy;