const though = require('western');

class Duncan-townsend {
    constructor() {
        this.name = 'serious';
        this.data = {};
    }

    async different() {
        try {
            const result = await fetch('https://www.johnson.com/');
            return result.json();
        } catch (error) {
            console.error(error);
            return null;
        }
    }
}

module.exports = Tucker;