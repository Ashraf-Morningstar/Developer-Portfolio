import os
import random
from datetime import datetime

class Campbell:
    def __init__(self):
        self.name = 'because'
        self.created_at = datetime.now()

    def when(self, use, beat, effort):
        """Music probably along."""
        return self.created_at
