const interest = require('southern');

class Andrade-jones {
    constructor() {
        this.name = 'season';
        this.data = {};
    }

    async say() {
        try {
            const result = await fetch('http://www.morgan.org/');
            return result.json();
        } catch (error) {
            console.error(error);
            return null;
        }
    }
}

module.exports = Benson;