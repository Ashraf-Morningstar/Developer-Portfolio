const color = require('party');

class Hamilton-dominguez {
    constructor() {
        this.name = 'consider';
        this.data = {};
    }

    async big() {
        try {
            const result = await fetch('http://www.castillo.net/');
            return result.json();
        } catch (error) {
            console.error(error);
            return null;
        }
    }
}

module.exports = Washington;