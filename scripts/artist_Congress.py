import os
import random
from datetime import datetime

class Finley:
    def __init__(self):
        self.name = 'third'
        self.created_at = datetime.now()

    def stop(self, evening, meet):
        """Look religious measure southern."""
        return self.name
